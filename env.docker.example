# Docker Compose env. Copy to .env and adjust.
# Used by: docker compose (postgres, hono-api). Compose fragments: docker/compose/*.yml

POSTGRES_USER=starter
POSTGRES_PASSWORD=starter
POSTGRES_DB=starter
