diff --git a/dist/commonjs/compiler/lightningcss-loader.js b/dist/commonjs/compiler/lightningcss-loader.js
index a698a339aff0e01e4a1562861d136a395c412421..a0de427375ffbc4372ceb8f06a3bb66c330779a0 100644
--- a/dist/commonjs/compiler/lightningcss-loader.js
+++ b/dist/commonjs/compiler/lightningcss-loader.js
@@ -8,10 +8,20 @@ exports.lightningcssLoader = lightningcssLoader;
 function lightningcssLoader() {
   let lightningcssPath;

+  // Prefer the app workspace's lightningcss (NativeWind pins 1.30.1). @expo/metro-config
+  // depends on ^1.30.1 which can resolve to a newer 1.x that breaks react-native-css.
+  try {
+    lightningcssPath = require.resolve("lightningcss", {
+      paths: [process.cwd()]
+    });
+  } catch {
+    // Intentionally left empty
+  }
+
   // Try to resolve the path to lightningcss from the @expo/metro-config package
   // lightningcss is a direct dependency of @expo/metro-config
   try {
-    lightningcssPath = require.resolve("lightningcss", {
+    lightningcssPath ??= require.resolve("lightningcss", {
       paths: [require.resolve("@expo/metro-config/package.json").replace("/package.json", "")]
     });
   } catch {
diff --git a/dist/module/compiler/lightningcss-loader.js b/dist/module/compiler/lightningcss-loader.js
index 10b0a53e6d3c9ef3652c18abc475fb7d77d12e26..d667367002849837f512100553738009ba27d5ac 100644
--- a/dist/module/compiler/lightningcss-loader.js
+++ b/dist/module/compiler/lightningcss-loader.js
@@ -4,10 +4,20 @@
 export function lightningcssLoader() {
   let lightningcssPath;

+  // Prefer the app workspace's lightningcss (NativeWind pins 1.30.1). @expo/metro-config
+  // depends on ^1.30.1 which can resolve to a newer 1.x that breaks react-native-css.
+  try {
+    lightningcssPath = require.resolve("lightningcss", {
+      paths: [process.cwd()]
+    });
+  } catch {
+    // Intentionally left empty
+  }
+
   // Try to resolve the path to lightningcss from the @expo/metro-config package
   // lightningcss is a direct dependency of @expo/metro-config
   try {
-    lightningcssPath = require.resolve("lightningcss", {
+    lightningcssPath ??= require.resolve("lightningcss", {
       paths: [require.resolve("@expo/metro-config/package.json").replace("/package.json", "")]
     });
   } catch {
