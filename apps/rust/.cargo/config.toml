# Cargo QoL
[build]
# Optional: lock to a target for consistent builds
# target = "x86_64-unknown-linux-gnu"

[alias]
b = "build"
r = "run"
t = "test"
c = "clippy"
f = "fmt"

# Run `cargo clippy` for linting; add [lints] in Cargo.toml if you want to tune clippy lints.
