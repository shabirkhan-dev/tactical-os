# Docs moved

Project documentation now lives in the **docs app**:

- Source: `apps/docs/content/docs/`
- Dev server: `bun --cwd apps/docs run dev`
- Browse: http://localhost:3000/docs

Agents: follow `AGENTS.md` and the docs app routes listed there (for example `/docs/production-roadmap`, `/docs/architecture`, `/docs/docker`).